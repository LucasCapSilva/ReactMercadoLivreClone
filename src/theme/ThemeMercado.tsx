import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#FFF159'
    },
    secondary: {
      main: '#EDF1F0'
    }
  },
});

export default theme;